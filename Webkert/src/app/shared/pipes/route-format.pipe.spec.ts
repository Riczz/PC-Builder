import { RouteFormatPipe } from './route-format.pipe';

describe('RouteFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
